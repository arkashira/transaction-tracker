# Transaction Tracker
A Python project for transaction monitoring and automation.

## Features
* Automated troubleshooting module
* Real-time status updates and notifications

## Usage
1. Install the project using `poetry install`
2. Run the tests using `poetry run pytest`
3. Use the `AutomatedTroubleshootingModule` class to add transactions and resolve common issues
