# Transaction Tracker
A simple transaction tracking system.

## Usage
1. Create a `TransactionTracker` instance.
2. Add transactions using `add_transaction`.
3. Get transactions using `get_transactions`.
4. Sort transactions using `sort_transactions`.
5. Resolve issues using `resolve_issue`.
6. Convert to JSON using `to_json`.

## Testing
Run tests using `pytest`.
